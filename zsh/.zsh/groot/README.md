## groot

Jump to the top level of the current git repository. Walks up the directory tree until it finds a `.git` directory, then changes to that directory. Prints an error and returns non-zero if no `.git` directory is found.